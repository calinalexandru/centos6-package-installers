## php-vcs
> This script is designed to serve as a version control system (VCS) for PHP.

## interactive install

Clone the repo first, then:
```bash
# set permissions
sudo chmod -R +x php

# run script
sh ./php/wizard.sh
```
